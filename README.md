# Personal Profile Web Page
# Semantic HTML & CSS Box Model Implementation

##  Project Overview
This project is a personal profile web page created using semantic HTML5 and external CSS.
The goal of this assignment is to understand:

Proper HTML document structure

Use of semantic elements for accessibility

CSS box model concepts (margin, border, padding, content)

Clean, scalable, and class-based styling

The project follows real-world web development best practices.

## Tools Used

VS Code – Code editor

Web Browser (Chrome / Edge / Firefox) – Testing & inspection

Browser Developer Tools – Box model visualization

## Task 2: CSS Box Model Styling

Box Model Concepts Used

Content: Text and images inside elements

Padding: Inner spacing within elements

Border: Visual boundary of elements

Margin: Space between elements

CSS Best Practices Followed

External style.css file linked using <link> tag

Global styling using cascading rules

Class-based styling (no inline CSS)

Reusable CSS classes

Visual hierarchy using spacing and typography

Responsive behavior for smaller screens

box-sizing: border-box for layout consistency

## 🎯 Task Objective (Task 3)
**Create a Responsive Layout Using Flexbox**

Key goals:
- Align navigation items using Flexbox
- Create responsive content cards
- Make the layout mobile-friendly
- Use `flex-wrap`, `justify-content`, and `align-items`
- Test responsiveness using browser DevTools

